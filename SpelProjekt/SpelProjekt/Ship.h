#pragma once
#include <string>
#include <iostream>
#include "Enemie.h"

using namespace std;

class Ship : public Enemie
{
private:

public:
	Ship(string name, string mail, string department, int salery);
	virtual~Ship();
};
#pragma once
#pragma once
#pragma once
